import { Colors } from '../../constans/Colors'

export const styles = {
  imageContainer: {
    marginRight: 15,
  },
  imageCircle: {
    width: '100%',
    height: '100%',
    borderRadius: '50%',
    borderWidth: 2,
    borderColor: Colors.white,
    alignItems: 'center',
    justifyContent: 'center',
  },
}
